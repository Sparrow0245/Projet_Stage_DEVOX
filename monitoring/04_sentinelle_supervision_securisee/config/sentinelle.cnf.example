# ===============================================================
# Modèle de configuration MySQL/MariaDB pour les scripts Collector
# Emplacement GitHub : monitoring/04_sentinelle_supervision_securisee/config/sentinelle.cnf.example
# Destination VM    : /etc/mysql/sentinelle.cnf (droits 600 root:root)
# ===============================================================

[client]
user = sentinelle
password = SentinelleSecurePass2026!
host = localhost
database = sentinelle
default-character-set = utf8mb4
